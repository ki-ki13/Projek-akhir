const defaultTheme = require('tailwindcss/defaultTheme')
module.exports = {
    theme: {
        extend: {
            fontFamily: {
                'allerta-stencil': ['"Allerta Stencil"', 'sans-serif'],
            }
        }
    }
};